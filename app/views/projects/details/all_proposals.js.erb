if(document.querySelector(".shortlisted__user"))
document.querySelector(".shortlisted__user").remove();
if(document.querySelector(".rejected__user"))
document.querySelector(".rejected__user").remove();
if(document.querySelector(".proposal"))
{
document.querySelector(".proposal").remove();
<%request.referrer%>;
}
document
.querySelector(".all__proposals")
.insertAdjacentHTML(
"afterbegin",
`<%=j render 'all_proposals', bids: @bids%>`
);

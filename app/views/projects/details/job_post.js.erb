if(document.querySelector(".review__proposals"))
document.querySelector(".review__proposals").remove();
if(document.querySelector(".project__post"))
document.querySelector(".project__post").remove();
document
.querySelector(".project__overview")
.insertAdjacentHTML(
"afterend",
`<%=j render 'job-posts', project: @project%>`
);
